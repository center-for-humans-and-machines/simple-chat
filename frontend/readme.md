# Frontend

The frontend is written in TypeScript using [React](https://react.dev/) and bundled with [Parcel JS](https://parceljs.org). Further info can be found in the [documentation](../docs/frontend.md).
